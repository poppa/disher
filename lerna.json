{
  "packages": ["packages/*"],
  "version": "0.0.1",
  "npmClient": "yarn",
  "useWorkspaces": "true",
  "command": {
    "publish": {
      "ignoreChanges": ["*.test.*", "*.md"],
      "message": "chore(release): publish"
    }
  }
}
